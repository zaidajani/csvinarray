const fs = require('fs');
const path = require('path');

function translate(csv, path) {
  fs.readFileSync(path.join(__dirname + path));
  let array = csv.toString().replace(/\n/g, '').split("\r");
  return array
}

